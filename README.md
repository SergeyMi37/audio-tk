# Голосовой Транскрибер

Приложение на Python с графическим интерфейсом Tkinter для транскрипции голоса с микрофона и копирования текста в буфер обмена Windows.

## Системные требования

- Windows 10/11
- Python 3.8 или выше
- Микрофон
- Подключение к интернету (для Google Speech API)

## Установка

### 1. Клонирование или скачивание

``` bash
git clone https://github.com/<user>/audio-tk
cd audio-tk
```

Create virtual environment (optional)
``` bash
python3 -m venv env
source env/bin/activate
```

Create virtual environment for Windows
``` bash
python -m venv env-win
source env-win/Scripts/activate
```

### 2. Установка зависимостей

Откройте командную строку (cmd) или PowerShell в папке с файлами и выполните:

```bash
pip install -r requirements.txt
```

#### Если возникла ошибка при установке PyAudio:

**Способ 1 — через pipwin:**
```bash
pip install pipwin
pipwin install pyaudio
```

**Способ 2 — скачать готовый wheel:**
1. Перейдите на https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
2. Скачайте файл, соответствующий вашей версии Python (например, `PyAudio‑0.2.13‑cp311‑cp311‑win_amd64.whl` для Python 3.11 64-bit)
3. Установите командой:
```bash
pip install PyAudio‑0.2.13‑cp311‑cp311‑win_amd64.whl
```

## Запуск

```bash
python voice_transcriber.py
```

Или двойной клик по файлу `voice_transcriber.py` (если Python ассоциирован с .py файлами).

## Использование

### Основные элементы интерфейса

| Элемент | Описание |
|---------|----------|
| **Индикатор** | Серый — готов, Красный — идёт запись |
| **Начать/Остановить запись** | Включает/выключает прослушивание микрофона |
| **Копировать текст** | Копирует весь текст в буфер обмена |
| **Выбор языка** | Язык распознавания (RU, EN, DE, FR, ES) |
| **Текстовое поле** | Отображает распознанный текст |
| **Очистить** | Удаляет весь текст из поля |
| **Автокопирование** | Автоматически копирует текст после распознавания |

### Пошаговая инструкция

1. **Запустите программу** — откроется окно приложения
2. **Выберите язык** — по умолчанию установлен русский (ru-RU)
3. **Нажмите "Начать запись"** — индикатор станет красным
4. **Говорите в микрофон** — программа автоматически распознаёт речь
5. **Текст появится в поле** — и скопируется в буфер обмена (если включено автокопирование)
6. **Нажмите "Остановить запись"** — когда закончите
7. **Вставьте текст** куда нужно через Ctrl+V

### Советы для лучшего распознавания

- Говорите чётко и не слишком быстро
- Используйте качественный микрофон
- Минимизируйте фоновый шум
- Делайте паузы между фразами

## Решение проблем

### «Микрофон не найден»
- Проверьте подключение микрофона
- Убедитесь, что микрофон выбран устройством по умолчанию в настройках Windows
- Разрешите доступ к микрофону в настройках конфиденциальности Windows

### «Ошибка сервиса распознавания»
- Проверьте подключение к интернету
- Google Speech API может быть временно недоступен — попробуйте позже

### «Речь не распознана»
- Говорите громче и чётче
- Убедитесь, что выбран правильный язык
- Проверьте, что микрофон работает (индикатор уровня в настройках Windows)

## Зависимости

| Библиотека | Версия | Назначение |
|------------|--------|------------|
| SpeechRecognition | >=3.10.0 | Распознавание речи |
| PyAudio | >=0.2.13 | Работа с микрофоном |
| pyperclip | >=1.8.2 | Работа с буфером обмена |

## Лицензия

Свободное использование (MIT License).

---

*Автор: MiniMax Agent*
